node_modules
out
release
dist
.DS_Store
.tools
*.log
.env
config/google-oauth.json
google-oauth.json
config/client_secret_*.json

# Cabinet Grotesk is fetched from Fontshare at install time (EULA forbids redistribution)
# run: node scripts/fetch-cabinet-fonts.mjs
src/renderer/public/fonts/cabinet-grotesk-*.woff2

# build artifacts
*.tsbuildinfo

# LA7 bundled llama.cpp runtime — fetched per-platform at package time (MIT, large binaries)
# run: npm run runtime:fetch
build/runtime/

# regenerable capture output (run scripts/capture-news-widget-shots.mjs)
agent_docs/screenshots/
# docs/screenshots/ ships in git — the README references all five shots
# (regenerate with `npm run shots:readme`)

# Cursor local config — .cursor/mcp.json holds a live MCP bearer token; never commit
.cursor/
